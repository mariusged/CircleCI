version: "1.0"

stages:
  - "build"

steps:
  build:
    title: Running docker image
    type: freestyle
    working_directory: /
    arguments:
      image: 'ubuntu:latest'
      commands:
        - docker version
        - echo "asdasd"
